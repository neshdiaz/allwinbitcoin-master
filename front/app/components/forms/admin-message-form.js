import ModelForm from './model-form';
import messageStatus from 'front/fixtures/message-status';
import messageStyles from 'front/fixtures/message-styles';

export default ModelForm.extend({
  messageStatus,
  messageStyles
});
